# Frozen discovery regression fixture — sagarika29/ai-system-architect

Read-only transcription of the first Design Partner diagnostic corpus:

- Repository: `https://github.com/sagarika29/ai-system-architect`
- Commit: `0797e2736a1b288ae136b8f093644e3cc2406f6f`
  (2026-08-08, "AI Boundaries with deterministic, LLM routing based on
  decisions, tests for checks added")
- Files transcribed byte-for-byte:
  - `docs/architecture-boundaries-base.md`
  - `docs/architecture-guide.md`
  - `docs/PRD.md`
  - `pyproject.toml` (the config-echo regression source)

The Design Partner repository itself is never modified. This fixture exists
because the production server Audit on that commit discovered a single
low-confidence "Project Config: pyproject.toml" pseudo-decision while the
human-reviewed corpus contains meaningful documented architectural boundaries
in the three Markdown documents above. The discovery regression must not
require reproducing any specific human-authored record count; it asserts
that meaningful documented boundaries are discovered with correct tiers,
source attribution, and no partner-specific hard-coding.

Regenerate only by re-copying from a re-clone of the same commit and
updating this provenance note.
