[project]
name = "audit-fixture-repo"
version = "0.1.0"
description = "Fixture repository for Mneme architecture audit testing"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=7.0",
    "ruff>=0.1.0",
]