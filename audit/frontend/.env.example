# Development environment variables
# Copy to .env.local for local overrides

# API base URL for the audit backend
# Unset = same-origin (/api/...)
# Dev = http://localhost:8001
VITE_API_BASE=http://localhost:8001

# Optional emergency kill switch. Production defaults to enabled, but the runtime still
# requires https://mnemehq.com exactly; local and preview hosts fail closed.
VITE_AUDIT_GTM_ENABLED=false
