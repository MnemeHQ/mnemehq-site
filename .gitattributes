# OFL notices must remain byte-identical to their upstream sources (no EOL conversion)
site/assets/fonts/licenses/* -text

# Vite's HTML transform must receive LF to avoid doubled CR on Windows builds.
audit/frontend/index.html text eol=lf
site/audit/workspace/index.html text eol=lf
